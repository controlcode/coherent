machine:
  python:
    version: 3.6.3
  post:
    - pyenv global 3.6.3

dependencies:
  pre:
    - pipenv --version
    - pipenv --where
    - pipenv --venv
    - pip install pipenv
    - pipenv install

test:
  override:
    - pipenv run python run_tests.py
