machine:
  python:
    version: 3.6.3
  post:
    - pyenv global 3.6.3

dependencies:
  pre:
    - pip install pipenv
    - pipenv --version
    - pipenv --where
    - pipenv --venv
    - pipenv graph

test:
  override:
    - python run_tests.py
